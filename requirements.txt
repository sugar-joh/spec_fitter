# Core dependencies (Python >= 3.10 required)
numpy>=1.24
matplotlib>=3.7
astropy>=5.3
scipy>=1.10

# Optional: cosmic-ray removal via lacosmic
# Requires a C compiler: on macOS run `xcode-select --install`,
# on Linux run `sudo apt install gcc`
# astroscrappy>=1.1
